services:
  db:
    ports:
      - "5432:5432"

  app:
    environment:
      RUST_BACKEND_BIND: "0.0.0.0:9000"
    ports:
      - "${ISCY_HTTP_PORT:-9000}:9000"
    volumes:
      - media_data:/app/media
