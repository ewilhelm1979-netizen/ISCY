ISCY_BACKEND_URL=https://iscy.example.org
ISCY_TENANT_ID=1
ISCY_AGENT_QUEUE_MAX_FILES=100

# Only required for the first successful enrollment. Remove it afterwards.
# ISCY_AGENT_ENROLLMENT_TOKEN=iscy_enroll_replace_me

# Optional when the enrollment token and device are bound to validated mTLS.
# ISCY_AGENT_MTLS_FINGERPRINT=sha256:replace_me
